<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Expenses

    <a data-toggle="collapse" href="#trip_expenses_new_form">
      Add expense
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @trip.expenses.each do |expense| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/expenses/<%= expense.id %>">
          <%= expense.trip_id %>
        </a>

        <a href="/delete_expense_from_trip/<%= expense.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="trip_expenses_new_form">
        <%= render partial: "trip_templates/trip_expenses_new_form" %>
      </div>
    </div>

  </ul>
</div>
