<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All itineraries
    </h1>

    <a href="/itineraries/new" class="btn btn-block btn-outline-secondary">
      Add a new itinerary
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "itinerary_templates/itineraries_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Traveler
        </th>

        <th>
          Arrival flight
        </th>

        <th>
          Departure flight
        </th>

        <th>
          Home airport
        </th>

        <th>
          Destination
        </th>

        <th>
          Trip
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @itineraries.each do |itinerary| %>
      <tr>
        <!-- Display itinerary.id start -->
        <td>
          <%= itinerary.id %>
        </td>
        <!-- Display itinerary.id end -->

        <!-- Display itinerary.traveler_id start -->
        <td>
          <% if itinerary.traveler.present? %>
            <a href="/travelers/<%= itinerary.traveler.id %>">
              <%= itinerary.traveler.email %>
            </a>
          <% end %>
        </td>
        <!-- Display itinerary.traveler_id end -->

        <!-- Display itinerary.arrival_flight start -->
        <td>
          <%= itinerary.arrival_flight %>
        </td>
        <!-- Display itinerary.arrival_flight end -->

        <!-- Display itinerary.departure_flight start -->
        <td>
          <%= itinerary.departure_flight %>
        </td>
        <!-- Display itinerary.departure_flight end -->

        <!-- Display itinerary.home_airport start -->
        <td>
          <%= itinerary.home_airport %>
        </td>
        <!-- Display itinerary.home_airport end -->

        <!-- Display itinerary.destination start -->
        <td>
          <%= itinerary.destination %>
        </td>
        <!-- Display itinerary.destination end -->

        <!-- Display itinerary.trip_id start -->
        <td>
          <% if itinerary.trip.present? %>
            <a href="/trips/<%= itinerary.trip.id %>">
              <%= itinerary.trip.destination %>
            </a>
          <% end %>
        </td>
        <!-- Display itinerary.trip_id end -->

        <!-- Display itinerary.created_at start -->
        <td>
          <%= time_ago_in_words(itinerary.created_at) %> ago
        </td>
        <!-- Display itinerary.created_at end -->

        <!-- Display itinerary.updated_at start -->
        <td>
          <%= time_ago_in_words(itinerary.updated_at) %> ago
        </td>
        <!-- Display itinerary.updated_at end -->

        <td>
          <a href="/itineraries/<%= itinerary.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @itineraries, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
